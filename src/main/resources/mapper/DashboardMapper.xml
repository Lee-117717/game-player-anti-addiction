<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.game.antidote.mapper.DashboardMapper">

    <!-- ================================================================ -->
    <!-- 数据集1: ds_kpi_today — 当日核心KPI指标 (1行, ~60ms)            -->
    <!-- 映射: KpiToday {totalActive, minorActive, atRiskCount, ...}      -->
    <!-- ================================================================ -->
    <select id="selectKpiToday" resultType="com.game.antidote.entity.KpiToday">
        <![CDATA[
        SELECT
            COUNT(DISTINCT player_id)                       AS total_active,
            COUNT(DISTINCT CASE WHEN is_minor = 1
                THEN player_id END)                         AS minor_active,
            COUNT(DISTINCT CASE WHEN risk_label > 0
                THEN player_id END)                         AS at_risk_count,
            COUNT(DISTINCT CASE WHEN risk_label = 3
                THEN player_id END)                         AS severe_count,
            ROUND(SUM(recharge_amount), 2)                  AS total_recharge,
            ROUND(AVG(online_duration_min), 0)              AS avg_online_min
        FROM dwd_game_player_behavior
        WHERE dt = #{latestDt}
        ]]>
    </select>

    <!-- ================================================================ -->
    <!-- 数据集2: ds_duration_dist — 在线时长分布 (6行, ~110ms)           -->
    <!-- 映射: DurationDist {durationRange, playerCount, recordCount, pct} -->
    <!-- ================================================================ -->
    <select id="selectDurationDist" resultType="com.game.antidote.entity.DurationDist">
        <![CDATA[
        SELECT
            CASE
                WHEN online_duration_min < 30   THEN '<30分钟'
                WHEN online_duration_min < 60   THEN '30~60分钟'
                WHEN online_duration_min < 120  THEN '1~2小时'
                WHEN online_duration_min < 180  THEN '2~3小时'
                WHEN online_duration_min < 240  THEN '3~4小时'
                ELSE                                 '4小时以上(超限)'
            END                                               AS duration_range,
            COUNT(DISTINCT player_id)                         AS player_count,
            COUNT(*)                                          AS record_count,
            ROUND(COUNT(DISTINCT player_id) * 100.0
                / SUM(COUNT(DISTINCT player_id)) OVER(), 1)   AS pct
        FROM dwd_game_player_behavior
        WHERE dt = #{latestDt}
        GROUP BY duration_range
        ORDER BY MIN(online_duration_min)
        ]]>
    </select>

    <!-- ================================================================ -->
    <!-- 数据集3: ds_risk_trend — 7天风险趋势 (7行, ~80ms)                -->
    <!-- 映射: RiskTrend {statDate, dau, level1Count, level2Count, ...}    -->
    <!-- ================================================================ -->
    <select id="selectRiskTrend" resultType="com.game.antidote.entity.RiskTrend">
        <![CDATA[
        SELECT
            dt                                                AS stat_date,
            COUNT(DISTINCT player_id)                         AS dau,
            COUNT(DISTINCT CASE WHEN risk_label = 1
                THEN player_id END)                           AS level1_count,
            COUNT(DISTINCT CASE WHEN risk_label = 2
                THEN player_id END)                           AS level2_count,
            COUNT(DISTINCT CASE WHEN risk_label = 3
                THEN player_id END)                           AS level3_count,
            ROUND(
                COUNT(DISTINCT CASE WHEN risk_label > 0 THEN player_id END)
                * 100.0 / NULLIF(COUNT(DISTINCT player_id), 0), 1
            )                                                 AS risk_rate_pct,
            ROUND(
                COUNT(DISTINCT CASE WHEN risk_label > 0 AND is_minor = 1
                    THEN player_id END)
                * 100.0 / NULLIF(COUNT(DISTINCT CASE WHEN is_minor = 1
                    THEN player_id END), 0), 1
            )                                                 AS minor_risk_rate_pct
        FROM dwd_game_player_behavior
        WHERE dt >= DATE_SUB(#{latestDt}, INTERVAL 6 DAY)
          AND dt <= #{latestDt}
        GROUP BY dt
        ORDER BY dt
        ]]>
    </select>

    <!-- ================================================================ -->
    <!-- 数据集4: ds_payment_analysis — 付费分析 (≤12行, <100ms)          -->
    <!-- 映射: PaymentAnalysis {accountType, rechargeRange, playerCount, totalAmount, avgAmount} -->
    <!-- ================================================================ -->
    <select id="selectPaymentAnalysis" resultType="com.game.antidote.entity.PaymentAnalysis">
        <![CDATA[
        SELECT
            account_type                                      AS account_type,
            CASE
                WHEN recharge_amount = 0                      THEN '未付费'
                WHEN recharge_amount <= 30                    THEN '1~30元'
                WHEN recharge_amount <= 100                   THEN '30~100元'
                WHEN recharge_amount <= 300                   THEN '100~300元'
                WHEN recharge_amount <= 1000                  THEN '300~1000元'
                ELSE                                               '1000元以上'
            END                                               AS recharge_range,
            COUNT(DISTINCT player_id)                         AS player_count,
            ROUND(SUM(recharge_amount), 2)                    AS total_amount,
            ROUND(AVG(recharge_amount), 2)                    AS avg_amount
        FROM dwd_game_player_behavior
        WHERE dt = #{latestDt}
        GROUP BY account_type, recharge_range
        ORDER BY account_type, MIN(recharge_amount)
        ]]>
    </select>

    <!-- ================================================================ -->
    <!-- 数据集5: ds_period_stats — 24小时时段统计 (≤24行, <100ms)        -->
    <!-- 映射: PeriodStats {hourOfDay, adultActive, minorActive, ...}      -->
    <!-- ================================================================ -->
    <select id="selectPeriodStats" resultType="com.game.antidote.entity.PeriodStats">
        <![CDATA[
        SELECT
            login_hour                                        AS hour_of_day,
            COUNT(DISTINCT CASE WHEN account_type = 'adult'
                THEN player_id END)                           AS adult_active,
            COUNT(DISTINCT CASE WHEN account_type = 'minor'
                THEN player_id END)                           AS minor_active,
            COUNT(DISTINCT player_id)                         AS total_active,
            IF(login_hour >= 22 OR login_hour < 8, '夜间', '非夜间')
                                                              AS night_flag
        FROM dwd_game_player_behavior
        WHERE dt = #{latestDt}
        GROUP BY login_hour
        ORDER BY login_hour
        ]]>
    </select>

    <!-- ================================================================ -->
    <!-- 数据集6: ds_risk_ranking — 高风险玩家Top10 (10行, <100ms)        -->
    <!-- 映射: RiskRanking {playerId, activeDays, totalHours, ...}         -->
    <!-- ================================================================ -->
    <select id="selectRiskRanking" resultType="com.game.antidote.entity.RiskRanking">
        <![CDATA[
        SELECT
            player_id                                         AS player_id,
            COUNT(DISTINCT dt)                                AS active_days,
            ROUND(SUM(online_duration_min) / 60, 1)           AS total_hours,
            ROUND(AVG(online_duration_min), 0)                AS avg_daily_min,
            SUM(recharge_amount)                              AS total_recharge,
            MAX(risk_label)                                   AS max_risk_level,
            COUNT(DISTINCT game_id)                           AS game_count
        FROM dwd_game_player_behavior
        WHERE is_minor = 1
          AND risk_label > 0
          AND dt >= DATE_SUB(#{latestDt}, INTERVAL 6 DAY)
        GROUP BY player_id
        ORDER BY max_risk_level DESC, total_hours DESC
        LIMIT 10
        ]]>
    </select>

    <!-- ================================================================ -->
    <!-- 数据集7: ds_violation_detail — 违规玩家明细 (≤100行, <100ms)      -->
    <!-- 映射: ViolationDetail {statDate, playerId, gameId, loginTime, ...} -->
    <!-- ================================================================ -->
    <select id="selectViolationDetail" resultType="com.game.antidote.entity.ViolationDetail">
        <![CDATA[
        SELECT
            dt                                                AS stat_date,
            player_id,
            game_id,
            login_time,
            logout_time,
            ROUND(online_duration_min, 0)                     AS online_min,
            login_ip,
            device_type,
            game_region,
            CASE risk_label
                WHEN 1 THEN '超时预警'
                WHEN 2 THEN '夜间违规'
                WHEN 3 THEN '重度沉迷'
            END                                               AS risk_type,
            recharge_amount,
            match_count
        FROM dwd_game_player_behavior
        WHERE risk_label > 0
          AND dt >= DATE_SUB(#{latestDt}, INTERVAL 1 DAY)
        ORDER BY risk_label DESC, online_duration_min DESC
        LIMIT 100
        ]]>
    </select>

    <!-- ================================================================ -->
    <!-- 数据集8: ds_minor_night_detail — 未成年夜间违规明细               -->
    <!-- 映射: MinorNightDetail {dt, playerId, loginTime, ...}             -->
    <!-- ================================================================ -->
    <select id="selectMinorNightDetail" resultType="com.game.antidote.entity.MinorNightDetail">
        <![CDATA[
        SELECT
            dt,
            player_id,
            login_time,
            logout_time,
            ROUND(online_duration_min, 0)                     AS online_min,
            login_hour,
            device_type,
            game_region,
            game_id,
            recharge_amount,
            CASE risk_label
                WHEN 2 THEN '二级违规(仅夜间)'
                WHEN 3 THEN '重度沉迷(夜间+超时)'
            END                                               AS risk_desc
        FROM dwd_game_player_behavior
        WHERE is_minor = 1
          AND risk_label IN (2, 3)
          AND dt >= DATE_SUB(#{latestDt}, INTERVAL 6 DAY)
        ORDER BY dt DESC, online_duration_min DESC
        ]]>
    </select>

    <!-- ================================================================ -->
    <!-- 数据集9: ds_player_detail — 指定玩家明细 (下钻弹窗)                -->
    <!-- 映射: MinorNightDetail {dt, playerId, loginTime, ...}             -->
    <!-- ================================================================ -->
    <select id="selectPlayerDetail" resultType="com.game.antidote.entity.MinorNightDetail">
        <![CDATA[
        SELECT
            dt,
            player_id,
            login_time,
            logout_time,
            ROUND(online_duration_min, 0)                     AS online_min,
            login_hour,
            device_type,
            game_region,
            game_id,
            recharge_amount,
            CASE risk_label
                WHEN 0 THEN '正常'
                WHEN 1 THEN '超时预警(>4h)'
                WHEN 2 THEN '夜间违规(22:00-08:00)'
                WHEN 3 THEN '重度沉迷(夜间+超时)'
            END                                               AS risk_desc
        FROM dwd_game_player_behavior
        WHERE player_id = #{playerId}
          AND dt >= DATE_SUB(#{latestDt}, INTERVAL 6 DAY)
        ORDER BY dt DESC, online_duration_min DESC
        ]]>
    </select>

</mapper>
